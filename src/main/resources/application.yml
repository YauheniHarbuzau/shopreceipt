spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/shop_receipt
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

cache:
  type: LRU
  capacity: 10